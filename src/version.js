// Single source of truth for the version label displayed in the welcome
// dialog. Bumped together with package.json / Cargo.toml / tauri.conf.json
// on every commit (manual triple-bump until we automate).
export const VERSION = '1.0.0-rc.16';

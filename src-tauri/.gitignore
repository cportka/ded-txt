target/
gen/
# Build outputs we don't track:
# - Cargo.lock can be re-resolved by CI; if reproducibility ever bites us
#   we can flip this and commit it.
# - icons/ is regenerated by `npm run gen:icons` from build/icon.svg.
Cargo.lock
icons/
# Tauri 2 generates these on first build; safe to ignore.
WixTools/
